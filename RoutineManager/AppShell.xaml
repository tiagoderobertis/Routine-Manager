<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RoutineManager.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RoutineManager"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Title="Inicio">
            <ShellContent
            Title="¡Bienvenido!"
            ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage" />
        </Tab>
        <Tab Title="To Do">
            <ShellContent
            Title="Not home"
            ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage" />
        </Tab>
        <Tab Title="Other">
            <ShellContent
            Title="Other"
            ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage" />
        </Tab>
    </TabBar>

</Shell>
